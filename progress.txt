## Codebase Patterns
- Use `python3` not `python` to run commands (macOS setup)
- Pre-existing mypy error in `src/billfox/extract/docling.py:66` (DocumentStream export) - not a blocker
- Test patterns: use `unittest.mock.patch("pydantic_ai.Agent")` to mock Agent creation, `AsyncMock` for async agent.run()
- Use `FakeRunResult(output=...)` dataclass pattern for mocking pydantic-ai run results
- All test classes in `tests/test_parse_llm.py` follow `TestLLMParser*` naming
- Backup providers follow async pattern: sync method `_backup_sync()` wrapped in `asyncio.to_thread()`
- Date folders use UTC and zero-padded month/day: `YYYY/MM/DD`
- Mock `datetime` in backup modules by patching `billfox.backup.<module>.datetime`
- Ollama models use `pydantic_ai.models.openai.OpenAIModel` + `pydantic_ai.providers.openai.OpenAIProvider` with `{base_url}/v1/`
- For lazy imports in `_get_agent()`, patch at source (`pydantic_ai.models.openai.OpenAIModel`) not at the importing module
- `split(":", 1)` on model string handles tags like `llama3.2:7b` correctly
- Circular imports: `backup.py` → `app.py` is circular; shared factory functions live in `backup.py`, imported by `app.py`
- `build_backup_from_config(provider, local_path)` is the shared factory for backup provider construction

---

## 2026-04-12 - US-001
- Already completed before this iteration (load API keys from .env)

---

## 2026-04-12 - US-002
- What was implemented: Added `anthropic = ["anthropic>=0.40"]` optional dependency extra to pyproject.toml, included it in the `all` extras group, added mypy override for anthropic module
- Files changed: pyproject.toml, tests/test_parse_llm.py
- **Learnings for future iterations:**
  - pydantic-ai routes `anthropic:` model strings natively - no code changes needed in LLMParser, just the dependency
  - When adding new optional deps, remember to also add mypy overrides if the package lacks type stubs
  - 247 tests all pass; 3 warnings from aiosqlite event loop cleanup are pre-existing

---

## 2026-04-12 - US-003
- What was implemented: LocalBackup class in `src/billfox/backup/local.py` implementing DocumentBackup protocol with date-based folder structure (YYYY/MM/DD), re-exported from `backup/__init__.py`
- Files changed: src/billfox/backup/local.py (new), src/billfox/backup/__init__.py, tests/test_backup_local.py (new)
- **Learnings for future iterations:**
  - `Path.write_bytes()` handles overwrite naturally - no need for explicit delete-then-write
  - `PurePath(source_uri).name` extracts filename from any path format
  - Test pattern: patch `billfox.backup.local.datetime` to control date in tests, set both `.now.return_value` and `.UTC`
  - 254 tests all pass after adding 7 new tests

---

## 2026-04-12 - US-004
- What was implemented: Added Ollama model prefix support to LLMParser. When model starts with `ollama:`, constructs pydantic-ai `OpenAIModel` with `OpenAIProvider(base_url='{base_url}/v1/')`. Added optional `base_url` parameter, defaults to `http://localhost:11434` for ollama models. Tags in model names (e.g. `llama3.2:7b`) handled via first-colon split.
- Files changed: src/billfox/parse/llm.py, tests/test_parse_llm.py
- **Learnings for future iterations:**
  - Lazy imports inside methods can't be patched at the importing module level (e.g. `billfox.parse.llm.OpenAIModel`) — patch at source (`pydantic_ai.models.openai.OpenAIModel`)
  - `split(":", 1)[1]` preserves everything after first colon, so `ollama:llama3.2:7b` → `llama3.2:7b`
  - 261 tests all pass after adding 7 new Ollama tests

---

## 2026-04-12 - US-005
- What was implemented: Wired backup provider selection into CLI backup command and parse command. Added `build_backup_from_config()` factory function in `cli/backup.py`. Backup command reads `defaults.backup.provider` from `~/.billfox/config.toml` and constructs LocalBackup or GoogleDriveBackup accordingly. Parse command with `--store` also wires up the configured backup provider via the Pipeline's backup parameter.
- Files changed: src/billfox/cli/backup.py, src/billfox/cli/app.py, tests/test_cli_backup.py (new)
- **Learnings for future iterations:**
  - Circular import avoidance: `backup.py` can't import from `app.py` since `app.py` imports from `backup.py`. Shared logic goes in `backup.py` and gets imported by `app.py`.
  - For lazy imports (inside functions), always patch at the source module path (e.g. `billfox.backup.google_drive.client.GoogleDriveBackup`), not the importing module.
  - `_read_backup_config()` helper in backup.py reads config independently using tomllib, avoiding dependency on app.py config helpers.
  - 272 tests all pass after adding 11 new tests

---

## 2026-04-12 - US-006
- What was implemented: Wired Ollama/LLM config into CLI parse command. Changed `--model` default from `"openai:gpt-4.1"` to `None`, added config resolution logic: reads `defaults.llm.provider`, `defaults.llm.model`, `defaults.ollama.model`, and `defaults.ollama.base_url` from `~/.billfox/config.toml`. When provider is `ollama`, constructs `ollama:{model}` string. `base_url` from config is passed to `LLMParser` for any `ollama:` model (including CLI overrides). Falls back to `openai:gpt-4.1` when no config and no CLI flag.
- Files changed: src/billfox/cli/app.py, tests/test_cli_parse_llm_config.py (new)
- **Learnings for future iterations:**
  - Config resolution order: CLI flag > config provider-specific logic > config `defaults.llm.model` > hardcoded fallback
  - `base_url` should be picked up from config even when user passes `--model ollama:something` via CLI (the model is overridden but the base URL is still useful)
  - Mocking pattern for parse command tests: patch `billfox.parse.llm.LLMParser` and `billfox.cli.app._read_config` together, assert on `LLMParser` call kwargs
  - 280 tests all pass after adding 8 new tests

---
