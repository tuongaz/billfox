# Ralph Progress Log
Started: Sat 11 Apr 2026 17:07:09 AEST

## Codebase Patterns
- Use `python3` not `python` — only python3 is available on this system
- Use `from __future__ import annotations` in all source files for forward references
- Protocols use `@runtime_checkable` decorator and live in `_base.py` files within each subpackage
- Covariant TypeVar (`T_co`) required for generic Protocol type parameters (mypy strict)
- Ruff TCH rules disabled — runtime imports needed for @runtime_checkable protocols and dataclass fields
- mypy overrides for third-party packages use separate `[[tool.mypy.overrides]]` blocks per module
- Each subpackage has `__init__.py` re-exporting public names via `__all__`
- Use `Image.Resampling.BILINEAR` not `Image.BILINEAR` (deprecated in Pillow>=10)
- Reference code uses cv2 but billfox uses Pillow — convert BGR↔RGB with `[:, :, ::-1]`
- Wrap sync SDK calls with `asyncio.to_thread()` for async protocol implementations
- Don't use `type: ignore[import-untyped]` for installed dev dependencies — mypy flags it as `unused-ignore`
- Lazy-imported client getters (`_get_client`) must return `Any`, not `object` — mypy can't see methods on `object`
- Use `datetime.UTC` not `datetime.timezone.utc` — ruff UP017 enforces the alias
- FTS5 content sync tables need manual `INSERT ... VALUES('delete', ...)` before row updates/deletes
- `event.listen(engine.sync_engine, "connect", callback)` for SQLite pragma setup with async engines
- FTS5 `bm25()` returns negative scores (lower = better); negate for fusion (higher = better)
- sqlite-vec vec0 `distance` is cosine distance; convert to similarity with `1 - distance`
- Vec0 row IDs use `document_id:field_name` format — split on `:` to extract document_id
---

## 2026-04-11 - US-001
- Implemented project scaffold with src/billfox/ layout
- Created pyproject.toml with hatch build backend, all optional extras (mistral, yolo, llm, openai, store, cli, all, dev)
- Core types: Document, Page, ExtractionResult, SearchResult as frozen dataclasses in _types.py
- Protocol definitions in _base.py: DocumentSource, Preprocessor, Extractor, Parser[T], Embedder, DocumentStore[T]
- Pipeline compositor in pipeline.py with run() and extract_only() methods
- Makefile, LICENSE (MIT), CHANGELOG.md, CONTRIBUTING.md, README.md
- Files changed: pyproject.toml, src/billfox/**, tests/__init__.py, Makefile, LICENSE, CHANGELOG.md, CONTRIBUTING.md, README.md
- **Learnings for future iterations:**
  - `python` command not available, must use `python3`
  - README.md must exist before hatch can build (it reads it for metadata)
  - Parser protocol TypeVar must be covariant (`T_co`) for mypy strict mode
  - Ruff TCH001 rule conflicts with runtime-checkable protocols — disabled TCH rules entirely
---

## 2026-04-11 - US-002
- Implemented `source/local.py` with `LocalFileSource` class matching `DocumentSource` protocol
- Supports MIME types: image/jpeg, image/png, image/webp, image/tiff, image/heic, application/pdf
- Uses `mimetypes.guess_type` for extension-based detection; registers .heic and .webp explicitly
- Raises `FileNotFoundError` for missing files, `ValueError` for unsupported types
- Updated `source/__init__.py` to re-export `LocalFileSource`
- 9 unit tests covering all MIME types, missing file, unsupported extension, and protocol conformance
- Files changed: src/billfox/source/local.py, src/billfox/source/__init__.py, tests/test_source_local.py
- **Learnings for future iterations:**
  - Python's `mimetypes` module doesn't know `.heic` or `.webp` by default — must register them with `mimetypes.add_type()`
  - `frozenset` works well for constant sets of allowed values
  - `tmp_path` pytest fixture returns a `pathlib.Path` — use directly for test file creation
---

## 2026-04-11 - US-003
- Implemented `preprocess/yolo.py` with `YOLOPreprocessor` matching `Preprocessor` protocol
- Ported from api/src/ai/yolo/receipt_onnx.py: `_preprocess`, `_postprocess`, `_nms`, `_compute_iou`, `_compute_containment`, `_crop_detections`
- Renamed from receipt-specific to generic document naming
- Uses Pillow (not cv2) for image I/O — matches the `yolo` extra dependencies
- Instance-level lazy ONNX session (not global), accepts user `model_path`
- Only processes image MIME types; PDFs pass through unchanged
- Returns original document if no detections found
- 19 unit tests: helper functions (IoU, containment, NMS, preprocess, postprocess) + integration tests with mocked ONNX session
- Files changed: src/billfox/preprocess/yolo.py, src/billfox/preprocess/__init__.py, tests/test_preprocess_yolo.py
- **Learnings for future iterations:**
  - Pillow `Image.BILINEAR` is deprecated; use `Image.Resampling.BILINEAR` for Pillow>=10
  - Reference code uses cv2, but billfox yolo extra uses Pillow — need to convert BGR/RGB manually with numpy slicing (`[:, :, ::-1]`)
  - Mock ONNX sessions by setting `preprocessor._session` directly — avoids needing real model files
  - YOLO output tensor shape is (1, 5, num_boxes) — transpose to (num_boxes, 5) for postprocessing
---

## 2026-04-11 - US-004
- Implemented `preprocess/resize.py` with `ResizePreprocessor` — resizes images maintaining aspect ratio using Pillow
- Implemented `preprocess/chain.py` with `PreprocessorChain` — chains multiple preprocessors sequentially
- Updated `preprocess/__init__.py` to export `ResizePreprocessor` and `PreprocessorChain`
- 9 resize tests + 6 chain tests = 15 new tests, all passing
- Files changed: src/billfox/preprocess/resize.py, src/billfox/preprocess/chain.py, src/billfox/preprocess/__init__.py, tests/test_preprocess_resize.py, tests/test_preprocess_chain.py
- **Learnings for future iterations:**
  - Pillow uses `Image.Resampling.LANCZOS` for downscaling (better quality than BILINEAR) and `Image.Resampling.BILINEAR` for upscaling
  - `img.format` on PIL Image returns the format string (e.g. "JPEG", "PNG") — use it to preserve format when re-encoding
  - When image is within limits, return the original `Document` object directly (`return document`) to avoid unnecessary re-encoding
  - `PreprocessorChain` is a simple but powerful pattern — implements `Preprocessor` protocol so it composes recursively
---

## 2026-04-11 - US-005
- Implemented `extract/mistral.py` with `MistralExtractor` matching `Extractor` protocol
- Ported from api/src/ai/agents/expense/mistral.py: `_to_data_url`, `_validate_mime`, OCR call logic
- Correctly branches `image_url` (images) vs `document_url` (PDFs) based on MIME type
- Uses `asyncio.to_thread` to wrap synchronous Mistral SDK calls for async protocol
- Lazy imports `mistralai` inside `_get_client()` with clear install instructions
- Constructor accepts optional `api_key` (falls back to `MISTRAL_API_KEY` env var) and `model` (default `mistral-ocr-latest`)
- Updated `extract/__init__.py` to export `MistralExtractor`
- 22 unit tests: helpers, init, extract with mocked client, protocol conformance
- Files changed: src/billfox/extract/mistral.py, src/billfox/extract/__init__.py, tests/test_extract_mistral.py
- **Learnings for future iterations:**
  - `mistralai` package is installed in dev env, so `type: ignore[import-untyped]` is unnecessary — mypy will flag it as `unused-ignore`
  - Mistral OCR `document` param uses `{"type": "image_url", "image_url": url}` for images and `{"type": "document_url", "document_url": url}` for PDFs
  - `asyncio.to_thread` is the simplest way to wrap sync SDK calls for async protocols
  - When mocking the client in tests, use `patch.object(extractor, "_get_client", return_value=mock_client)` to avoid needing the real mistralai package
  - Access call kwargs with `call_args.kwargs.get("key")` for keyword-only API calls
---

## 2026-04-11 - US-006
- Implemented `parse/llm.py` with `LLMParser[T]` matching `Parser[T]` protocol
- Generic wrapper around pydantic-ai `Agent` with structured output
- Constructor accepts: `model` (str), `output_type` (type[T] bound to BaseModel), `system_prompt` (str), optional `retries` (default 1)
- `_get_agent()` lazy-imports pydantic-ai with clear install instructions on ImportError
- `parse(markdown)` creates agent, runs it, returns `result.output` as type T
- Updated `parse/__init__.py` to export `LLMParser`
- 9 unit tests: init params, default retries, import error, parse output, markdown passthrough, optional fields, agent params, protocol conformance
- Files changed: src/billfox/parse/llm.py, src/billfox/parse/__init__.py, tests/test_parse_llm.py
- **Learnings for future iterations:**
  - `_get_agent()` must return `Any` (not `object`) since pydantic_ai is lazy-imported — mypy can't see `.run()` on `object`
  - Use `# type: ignore[no-any-return]` for `result.output` since the return type flows through `Any`
  - pydantic-ai `Agent` constructor uses `system_prompt` (str), `output_type`, and `retries` — matches the reference codebase pattern exactly
  - Mock the agent via `patch.object(parser, "_get_agent", return_value=mock_agent)` to avoid needing real LLM calls
  - `ruff` I001 import-sorting rule is strict — always run `ruff check --fix` on new test files
---

## 2026-04-11 - US-007
- Added integration tests for `Pipeline[T]` compositor (pipeline.py already existed from US-001)
- 9 tests covering: full call order verification, no preprocessors, no store, store not called without document_id, multiple preprocessors in order, extract_only returns ExtractionResult, extract_only skips parse/store, extract_only applies preprocessors
- Files changed: tests/test_pipeline.py
- **Learnings for future iterations:**
  - Pipeline was already implemented in US-001 — this story was primarily about adding integration tests
  - AsyncMock works well for testing async protocol implementations — set `mock.method = AsyncMock(return_value=...)` explicitly
  - Don't import `call` or `SearchResult` unless actually used — ruff F401 catches this
---

## 2026-04-11 - US-008
- Implemented `embed/openai.py` with `OpenAIEmbedder` matching `Embedder` protocol
- Ported from api/src/ai/embeddings.py: parallel embedding via `asyncio.gather`, `encode_vector`/`decode_vector` base64 float packing
- Constructor accepts optional `api_key` (falls back to `OPENAI_API_KEY` env var) and `model` (default `text-embedding-3-small`)
- `dimensions` property returns known dimensions for OpenAI models (1536 for small/ada, 3072 for large)
- Lazy imports `openai` inside `_get_client()` with clear install instructions
- Updated `embed/__init__.py` to export `OpenAIEmbedder`, `encode_vector`, `decode_vector`
- 15 unit tests: constructor, dimensions, embed with mocks, encode/decode roundtrip, import error, protocol conformance
- Files changed: src/billfox/embed/openai.py, src/billfox/embed/__init__.py, tests/test_embed_openai.py
- **Learnings for future iterations:**
  - `_get_client()` must return `Any` (not `object`) for lazy-imported clients — same pattern as `parse/llm.py`
  - `asyncio.gather` with inner coroutines is the standard pattern for parallel API calls
  - `struct.pack/unpack` with `f` format gives float32 — matches the reference codebase exactly
  - Ruff I001 auto-fix with `--fix` handles import sorting; B905 requires manual `strict=True` on `zip()`
---

## 2026-04-11 - US-009
- Implemented `store/_vector.py` with `VectorType` — SQLite-only SQLAlchemy TypeDecorator (float32 struct packing)
- Implemented `store/_schema.py` with `DocumentRow` and `DocumentEmbeddingRow` ORM models
- Implemented `store/sqlite.py` with `SQLiteDocumentStore[T]` matching `DocumentStore[T]` protocol
- Auto-creates tables on first use: core tables + FTS5 virtual table + optional sqlite-vec vec0 table
- SQLite WAL mode, foreign_keys=ON, busy_timeout=5000 configured via connection event
- sqlite-vec loaded via `sqlite_vec.load()` with graceful fallback
- save/get/delete CRUD with Pydantic JSON serialization
- Embedding sync to FTS5 and vec0 tables when `embed_fields` provided
- search() is a placeholder returning `[]` — full implementation in US-010
- Updated `store/__init__.py` to export `SQLiteDocumentStore` and `VectorType`
- 10 integration tests with in-memory SQLite: save, get, overwrite, delete, multiple docs, cascade, schema_name
- Files changed: src/billfox/store/_vector.py, src/billfox/store/_schema.py, src/billfox/store/sqlite.py, src/billfox/store/__init__.py, tests/test_store_sqlite.py
- **Learnings for future iterations:**
  - Use `datetime.UTC` not `datetime.timezone.utc` — ruff UP017 enforces this
  - `async with session_factory() as session, session.begin():` — ruff SIM117 requires combining nested async with
  - FTS5 content sync requires manual INSERT with `'delete'` command before updating/deleting rows
  - `contextlib.suppress(Exception)` preferred over bare `try/except/pass` — ruff SIM105
  - VectorType is much simpler when SQLite-only — no need for `load_dialect_impl` branching
  - `event.listen(engine.sync_engine, "connect", callback)` is the pattern for SQLite pragma setup with async engines
---

## 2026-04-11 - US-010
- Implemented `store/_search.py` with hybrid search pipeline
- `normalize_scores()` — min-max normalization to [0,1] ported from reference
- `apply_hybrid_fusion()` — 60% normalized + 40% RRF (k=60) with recency tie-breaking
- `bm25_search()` — FTS5 full-text search via `bm25()` scoring function
- `vector_knn_search()` — sqlite-vec KNN via `MATCH` on vec0 virtual table
- `hybrid_search()` — orchestrator combining BM25 + vector signals
- Updated `store/sqlite.py` `search()` to call hybrid_search and return SearchResult objects
- 20 tests: 5 normalize_scores unit, 6 fusion unit, 9 integration with in-memory SQLite
- Files changed: src/billfox/store/_search.py (new), src/billfox/store/sqlite.py, tests/test_store_search.py (new)
- **Learnings for future iterations:**
  - FTS5 `bm25()` returns negative scores (lower = better match) — must negate for fusion convention
  - sqlite-vec vec0 returns cosine `distance`, not similarity — use `1 - distance`
  - Vec0 row IDs follow `document_id:field_name` format from _sync_embeddings — split with `rsplit(":", 1)[0]` to extract doc ID
  - FTS5 MATCH queries use simple terms, not SQL LIKE — just pass the query text directly
  - Dynamic SQL placeholders for IN clauses need `:id0, :id1, ...` pattern with matching param dict
  - The `_search.py` module is internal (`_` prefix) — no need to export from `__init__.py`
---
